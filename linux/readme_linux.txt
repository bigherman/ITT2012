The linux part of the project will include:
	1. Scripts to fetch a large text file, containing weather reports from NOAA using FTP (wget/curl)
	2. PHP and MySQL code to parse the fetched file and to add individual reports to a database
	3. PHP code to respond to REST web service requests, which may come from iOS/Android Apps, browsers or any other type of network enabled application